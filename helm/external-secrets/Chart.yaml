apiVersion: v2
name: external-secrets
description: Tool to install external-secrets in clusters.
home: https://github.com/giantswarm/external-secrets
version: "0.2.1"
appVersion: "v0.7.0"
# If you have an icon/logo, you should add it to https://github.com/giantswarm/web-assets
# and set the final URL as a value here and uncomment.
# icon: https://s.giantswarm.io/app-icons/example/1/light.svg
annotations:
  application.giantswarm.io/team: "honeybadger"
  config.giantswarm.io/version: 1.x.x
restrictions:
  clusterSingleton: true
  namespaceSingleton: true
